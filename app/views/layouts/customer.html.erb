<!DOCTYPE html>
<html>
  <head>
    <title>Customer Page</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .nav-item {
        width: 50%;
        text-align: center;
      }
    </style>
  </head>
  <nav class="navbar bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand">Customer</a>
      <form class="d-flex" role="search">
        <%= link_to "Logout", destroy_customer_session_path, data: {turbo_method: :delete} , class:"btn btn-outline-success" %>
      </form>
    </div>
  </nav>
  <div class="container mt-1">
    <ul class="nav nav-tabs mb-">
      <li class="nav-item">
        <a class="nav-link" aria-current="page" href="<%= customers_teachers_path %>">Teachers</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" aria-current="page" href="<%= customers_students_path %>">Students</a>
      </li>
    </ul>
    <% if @q != nil  %>
      <%= search_form_for [:customers,@q] do |search_fields| %>
        <%= render "customers/partial/search", search_fields: search_fields %>
      <% end %>
    <% end %>
    <%= yield %>
  </div>
</html>
